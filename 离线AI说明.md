# 离线AI课堂

## 上课时怎么打开

在已经准备好模型的这台 Mac 上，双击同目录的 **启动离线AI课堂.command**。模型加载完后会打开本机网页。保持启动窗口运行，课堂结束在该窗口按 Control+C。

本机地址：http://127.0.0.1:8767/?offline-ai=1

在课程地图第5部分选择“AI辅助表达”。页面显示“本机离线AI”才表示实时模型可用。两轮完成后，由学生选择、修改建议；“停止并清空”结束演示。课前应在实际投影设备上完整演练两轮，并关闭Wi-Fi再试一次。

普通GitHub网页或单文件离线课件仍可使用原课程、音视频和小游戏，但它们本身不能运行这台电脑的AI。需要真实离线回答时，请使用上面的启动入口。不是把预设答案伪装成AI。

## 三分钟现场流程

- 30秒：老师介绍虚构角色小宁。小组展示时被打断，有点生气，希望把话说完。不谈真实学生的隐私；可以旁观。
- 60—90秒：一名志愿者扮演小宁，老师陪同输入，两轮AI回应。其他学生带着“回应感受了吗、请求具体安全吗”观察。
- 30秒：志愿者说“我保留……，我想改成……”。
- 30秒：全班判断一句建议。强调AI可以理解错、建议可以拒绝，真实困难可以找可信任的大人。

无人自愿：老师扮演小宁，学生仍负责判断。模型故障：直接转同桌练习，明确说明没有使用AI。

本环节安排在35—38分钟，位于两轮同桌表达之后。保留两次各3分钟的表达练习；反馈、方法说明、最后对照各从2分钟缩短为1分钟，课程仍为50分钟。

## 本机准备与运行范围

- 当前实现面向 Apple Silicon Mac，采用 MLX LM 与 Qwen2.5-1.5B-Instruct 的4位量化模型；其他电脑需要单独准备和验证。
- 首次安装依赖与下载模型需要联网；完成后加载模型和生成文字均使用本地文件。
- 程序在运行时阻止外部网络连接，只监听本机127.0.0.1；无外部AI接口、无API密钥、无对话文件记录。
- 对话只保存在内存；停止、离开AI环节、重新开始或退出服务时清除。服务器15分钟后拒绝继续旧会话；新会话清空旧对话。
- 课堂为文字输入、文字回应，不包含离线语音识别或新配音。模型可能出错，需老师现场主持。这个小模型仍可能给出泛泛的表达，不能当作个人心理咨询入口。
- 生成后会检查已发现的角色混淆、说教措辞和请求是否具体，最多重试三次；不通过就显示未完成，不拿预设话术替代。检查规则不能保证内容正确。
- 辱骂或报复请求会显示明确的课堂规则提示，不将规则提示标成模型生成的回应。危险关键词会触发停止提示，但关键词规则不能识别所有风险；学生透露真实困扰时，老师应主动停止公开演示并提供适当支持。
- 请保留当前“初中生讲课”目录及其隐藏的 `.build` 文件夹。运行环境在 `.build/emotion-agent-runtime`，模型在 `.build/emotion-agent-model`。单独复制HTML不包含本地AI。

## 开源组件

- MLX LM：https://github.com/ml-explore/mlx-lm
- 模型：https://huggingface.co/mlx-community/Qwen2.5-1.5B-Instruct-4bit
- 固定模型版本：8b403126fc14f14cfc99bb4cfa72ecbc129ea677
- 模型上游：https://huggingface.co/Qwen/Qwen2.5-1.5B-Instruct
- 模型许可：Apache 2.0。GitHub课堂仓库不包含模型权重、运行环境或学生对话。
